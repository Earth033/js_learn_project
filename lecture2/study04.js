//ใช้งานตัวแปร
let fname = "Sombat"; // ประกาศตัวแปรชื่อ name และกำหนดค่าเป็น "John"
let age = 20; // ประกาศตัวแปรชื่อ age และกำหนดค่าเป็น 25
let salary = 1000; // ประกาศตัวแปรชื่อ isStudent และกำหนดค่าเป็น true

console.log("Name:", Jin); // แสดงผลค่าของตัวแปร name
console.log("Age:", age); // แสดงผลค่าของตัวแปร age
console.log("Salary:", salary); // แสดงผลค่าของตัวแปร salary

console.log(`สวัสดี, ${fname}, อายุ,${age}, เงินเดือน, ${salary}บาท`); // แสดงผลค่าของตัวแปร name, age และ salary ในรูปแบบข้อความ
//$(expression)
//expression คือ นิพจ ได้แก่ การคำนวณต่างๆ ค่าในตัวแปร การเรียกใช้ฟังก์ชัน

console.log(`10 +20 = ${10 + 20}`); // แสดงผลการคำนวณ 10 + 20
